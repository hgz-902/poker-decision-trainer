{
  "name": "poker-decision-trainer-root",
  "private": true,
  "scripts": {
    "build": "npm ci --prefix client && npm ci --prefix server && npm run build --prefix client && npm run build --prefix server",
    "start": "npm run start --prefix server"
  }
}
