{
  "name": "poker-decision-trainer-root",
  "private": true,
  "workspaces": ["client", "server"],
  "scripts": {
    "build": "npm -w client install && npm -w server install && npm -w client run build && npm -w server run build",
    "start": "npm -w server run start"
  }
}
